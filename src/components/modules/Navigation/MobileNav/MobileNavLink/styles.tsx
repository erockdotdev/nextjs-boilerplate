import styled from "styled-components";

export const MobileNavLinkStyles = styled.li`
  list-style-type: none;
  padding: 1em;
  &:last-of-type {
    padding-right: 0;
  }
  a {
    color: var(--white);
  }
`;
