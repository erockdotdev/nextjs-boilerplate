import styled from "styled-components";

export const NavStyles = styled.nav`
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1rem var(--gutter);
`;

export const NavLinkULStyles = styled.ul`
  margin: 0;
  padding: 0;
  display: flex;
  justify-content: flex-end;
`;
